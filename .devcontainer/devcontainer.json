// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
	"name": "Debian",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/base:bullseye",
	"features": {
		"ghcr.io/devcontainers/features/nvidia-cuda:1": {
			"installToolkit": true,
			"cudaVersion": "12.2",
			"cudnnVersion": "8.6.0.163"
		},
		"ghcr.io/devcontainers/features/python:1": {
			"installTools": true,
			"version": "os-provided"
		}
	},
	"postCreateCommand": "bash .devcontainer/post_create.sh",
	"runArgs": [
		"--gpus",
		"all"
	],
	// Make pip cache persistent so we don't have to download packages every time. Mount at ./.devcontainer/.pip-cache
	"mounts": [
		"source=${localWorkspaceFolder}/.devcontainer/.pip-cache,target=/home/vscode/.cache/pip,type=bind,consistency=cached"
	]
}
